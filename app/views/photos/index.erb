<header>
<div class="navbar navbar-default navbar-fixed-top" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><a href="#">Link</a></li>
        <li><a href="#">Link</a></li>
      </ul>
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
      </form>
      <ul class="nav navbar-nav navbar-right">
       <button type="submit" class="btn btn-default">Submit</button>
      </ul>
    </div>
  </div>
</nav>
</header>

<body class="home">
<h3>Hello <%= @current_user.username %> User: <%= @current_user.id %></h3>


<form method="post" action="/logout">
  <input type="submit" value="Logout">
</form>


<button id="newBtn">New</button>
<button id="profBtn">My Profile</button>

<br>
<p>Today's photo theme is: <%= @theme.name %></p>
<br>

<div class="gallery cf row">
  <% @photos.order(created_at: :desc).each do |photo| %>
    <a href="/photos/<%= photo.id %>">
      <div class="photo-container col-md-4" style="background-image: url(<%= photo.url %>)">
      </div>
    </a>
  <% end %>
</div>

</body>

